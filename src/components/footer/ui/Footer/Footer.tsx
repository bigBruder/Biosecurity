import { BottomFooter } from '../BottomFooter';
import { Layout } from '../Layout';
import { TopFooter } from '../TopFooter';

const Footer = () => <Layout topFooter={<TopFooter />} bottomFooter={<BottomFooter />} />;

export { Footer };
